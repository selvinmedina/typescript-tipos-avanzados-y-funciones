const prices: (number | string | boolean)[] = [1, 2, 3, 5, 6, 'asd', true];

// prices.push({});
prices.push(1);
prices.push("saludos");
prices.push(false);
// prices.push([]);

prices

let user:[string, number] = ['selvinmedina', 21];

const [userName, agre] = user;

userName
agre
